```
양강사님 
- 오전: 참조자료형부터 ~ 

```

## 장애 이야기
```text
dns(우회 하는 것도 여기서 해줌)
백본 라우터
오로라(aws: rds / 오로라 추천 *돈이들어도 복제 지연이 발생하지 않음) 
- 앞에서 부하가 많이 걸리면 뒤에서 처리가 안 됨
클러스터링
- max transaction이라던지, 설정값 요소들
- default 설정보다 높으면, 네트워크에서 응답을 하지 않음. 
ai rag 공부해놓기

pc - dns - 라우터 - fw(firewall) - l4(load balancer) - 서버1 (내부에 app) - DB(MySQL)w - R1  
				            	- 서버2 (내부에 app)			  - R2(rds의 프록시)
- 부하가 많을 경우엔 문제가 일어남. 
- 네트워크 장비는 문제가 잘 안일어남. 
```
## 용어
- ai rag 공부해놓기
- log4J - system.out.println 간혹 찍으면 안된다.  --> 굉장한 부하다. 
- 최소한의 것들을 찍고, 하나씩 늘려나가기.  --> 뭐가 장애가 난건지 알 수가 없다. 
- OM : outOfMemory
- 성능 튜닝 --> tracking 문제있어요 (몸값 높음)
- 클러스터링
- 오로라
- vpc
- msa(micro service architecture)
- ec2
- log4j, slf4j
- 롤링시키다
- firewall
- 백본라우터
- 코드 프로파일링
- 팩토리 메서드
- 오브젝트 풀링
- 웹표준
- 메서드 시그니처: 메서드 이름/ 매개변수 타입 
- 오버로딩: 메서드 이름이 동일하더라도 메서드 시그너처가 다르면 다른 메서드로 인식 
- autonomousDB (oracle): 알아서 튜닝을 해서 실행 
- 유스케이스 다이어그램
  - 유스케이스 명세서: usecase specification
  


## 오전
- 배열: 오류가 날 수 있기 때문에 대체로 Collection을 써보자. 
- 요일, 개월: 변경될 일 없음
>- new 키워드
>- “Heap 메모리에 넣어라＂의 의미
> - JVM이 비워있는 공간에 객체를 생성
> - 어느 공간에 넣었는지 알 수가 없기 때문에 JVM은
> 객체의 위치정보를 리턴해주어야 함
> - 리턴된 값을 참조변수에 저장
- 힙 메모리에 차게 되면, outOfMemory

### Stack 메모리의 초기값
- 초기값을 부여하지 않는 경우 빈칸으로 존재
(읽기 불가능)
int a; → stack 메모리에 공간 생성 (빈칸으로 존재)
System.out.println(a); → 오류발생
Heap 메모리의 초기값
- 빈칸으로 존재할 수 없으며 디폴트 초기값이 강제 설정
- 기본자료형
숫자(int, double 등) 디폴트값 : 0
boolean 디폴트: `false`
- 참조자료형 디폴트값: `null`

### 1차원 배열 
- ‘=‘는 stack 메모리의 값을 복사
- 기본자료형의 변수 복사: 값의 복사
- 참조자료형의 변수 복사: 위치(번지) 복사

### String
- equals():  문자열의 실제 내용비교 (cf. ==는 메모리번지(stack) 비교)
- equalsIgnoreCase(String anotherString) : 대소문자 구분없이 문자열의 실제 내용 비교
```java
String str1 = new String("안녕");
String str2 = str1;
str1 = "안녕하세요";
System.out.println(str1); //안녕하세요
System.out.println(str2); //안녕
```
### 2가지 특징 
- 특징 1. 객체내의 값 변경 불가능 → 값 변경시 새로운 객체를 생성하여 작성
- 특징 2. 리터럴을 바로 입력한 데이터는 문자열이 같은 경우 하나의 객체를 공유

---
# 오후 
## 클래스의 기본 문법
- 자바가 객체를 생성하는 가장 근간. 붕어빵 틀 
- 실행을 할 시점. --> 인스턴스가 생김 
- 클래스: 멤버변수, behavior(메소드)
- 객체는 클래스의 인스턴스이다. 

## 절차지향형 
- 순서에 맞춰 단계적으로 실행하도록 명령어를 나열 (ex: C)
- 사물의 모든 현상에 대해 시간 순서대로 벌어지는 것으로 모델링 된 것.

## 객체지향형
- 패러다임은 좋고 나쁜 것이 아니다. 방법론이라고 생각. 
- 객체와 객체간의 interaction 접근법

## 추상 클래스와 인터페이스
- 추상클래스: 추상(abstract) 메서드를 하나 이상 가지고 있는 클래스
  - 함수의 정의가 미완성된 메서드.
- 인터페이스: 모든 필드는 public static final, 모든 메서드는 public
abstract
  - 메소드만 있음. 
- new를 가급적이면 


